<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- GPS location for SOS. Precise + coarse: geolocator requires both on
         Android and only while-in-use is used (no background location). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Audio recording for SOS evidence (runtime-granted at SOS time). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Distress vibration pattern. -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Deliberately ABSENT permissions (least privilege):
         - CALL_PHONE / SEND_SMS: the app opens the dialer (ACTION_DIAL) and
           the SMS composer (ACTION_SENDTO sms:) — neither requires these.
         - ACCESS_BACKGROUND_LOCATION: SOS reads location only while in use.
         - POST_NOTIFICATIONS: no notification feature exists.
         - WRITE_EXTERNAL_STORAGE: audio is written to app-private storage.
    -->

    <application
        android:label="she_sheild"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- SMS composer used by the SOS contact notification action -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="sms"/>
        </intent>
        <!-- Phone dialer used by the SOS primary-contact call action -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
        </intent>
    </queries>
</manifest>
